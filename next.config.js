/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['apod.nasa.gov', 'www.youtube.com', 'images.pexels.com', 'player.vimeo.com', 'images-assets.nasa.gov'],
  },
};

module.exports = nextConfig;
